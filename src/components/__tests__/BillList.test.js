describe('the Bill List', () =>{
  it('is contained in an element with the class name bill-list', ()=>{
    // render component 

    // check if component has classname bill-list
  });

  it('displays a <li> element for each bill', ()=>{
    // create a fake bill search 

    // check to see if each bill is rendered as a <li> element
  });
});